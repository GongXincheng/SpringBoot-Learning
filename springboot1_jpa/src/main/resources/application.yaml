# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /


# 数据源
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/test

  # JPA
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

